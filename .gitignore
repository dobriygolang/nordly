# optional monorepo workspace
go.work.sum
.cache/
.vite/
.vite/

# prod secrets
deploy/.env
deploy/secrets/
deploy/backups/
deploy/.proto-bin/

# each service ignores its own pkg/api/ and .bin/ via services/*/.gitignore
.DS_Store
