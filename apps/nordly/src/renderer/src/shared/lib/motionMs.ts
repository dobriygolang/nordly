/** Keep in sync with `--motion-dur-*` in app/styles/components/chrome.css (Winter 0.0.27). */
export const MOTION_MS = {
  micro: 100,
  small: 150,
  medium: 200,
  page: 400,
  content: 500,
  poster: 700,
  large: 360,
  long: 640,
} as const;
