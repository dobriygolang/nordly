# One-time cutover: reuse druzya-prod Postgres volume (data preserved).
# Usage: docker compose -f docker-compose.prod.yml -f docker-compose.migrate-volumes.yml ...
volumes:
  postgres_data:
    external: true
    name: druzya-prod_postgres_data
