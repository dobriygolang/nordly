---
description: Go backend — identity service
globs: "**/*.go"
alwaysApply: false
---

# Go backend — identity

Follow [AGENTS.md](../../AGENTS.md), root [fail-fast-no-fallbacks.mdc](../../../../.cursor/rules/fail-fast-no-fallbacks.mdc), and [go-backend.mdc](../../../../.cursor/rules/go-backend.mdc).

- `TELEGRAM_BOT_TOKEN`, `TELEGRAM_BOT_USERNAME`, JWT keys required in `config.Load()`.
- Auth flows use explicit `ErrNotFound` branches — not `user == nil` without error.
