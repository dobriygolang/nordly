---
description: Go backend — tracker service
globs: "**/*.go"
alwaysApply: false
---

# Go backend — tracker

Follow [AGENTS.md](../../AGENTS.md), root [fail-fast-no-fallbacks.mdc](../../../../.cursor/rules/fail-fast-no-fallbacks.mdc),
[architecture-standard.mdc](../../../../.cursor/rules/architecture-standard.mdc), and [go-backend.mdc](../../../../.cursor/rules/go-backend.mdc).

- Required: `TOKEN_ENCRYPTION_KEY` and `NORDLY_CALLBACK_URL` in `config.Load()`.
- `service.New` returns an error if `Repo`, `Cipher`, `Google`, `Zoom`, or `Now` is missing (cipher always built in `run.go`).
- Google/Zoom: optional when OAuth env unset — use `Configured()`, not nil guards on wired deps.
